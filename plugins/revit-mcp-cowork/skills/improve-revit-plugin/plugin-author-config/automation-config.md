# Revit Learning Automation Config

`weekly_revit_plugin_improvement`

- Name: Revit Plugin Weekly Improvement
- Frequency: weekly on Mondays at 11:00 AM local time.
- Instructions: Follow `$revit-mcp-cowork:improve-revit-plugin` in scheduled weekly mode. Review only Revit-related Codex task history since the successful local date checkpoint, corroborate findings with sanitized hook evidence, and update the single bounded `revit-mcp-local-guidance` user skill only when its evidence and validation gates pass. Otherwise report no change. Never modify the installed plugin cache, create another learned skill, use Git or GitHub, publish anything, or advance the checkpoint after an incomplete scan.
