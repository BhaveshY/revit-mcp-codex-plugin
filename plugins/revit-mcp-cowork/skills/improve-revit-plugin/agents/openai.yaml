interface:
  display_name: "Local Revit Learning"
  short_description: "Set up and run verified local learning"
  default_prompt: "Use $improve-revit-plugin to set up or run verified local learning from Revit-related Codex tasks."
policy:
  allow_implicit_invocation: false
