# Revit Learning Automation Config

`weekly_revit_plugin_improvement`

- Name: Revit Plugin Weekly Improvement
- Frequency: weekly on Mondays at 11:00 AM local time.
- Instructions: Follow `$revit-mcp-cowork:improve-revit-plugin` in scheduled weekly mode. Use the dedicated task's writable `BhaveshY/revit-mcp-codex-plugin` checkout, review only Revit-related Codex task history since the successful date checkpoint, corroborate findings with sanitized hook evidence, and create at most a reviewable draft PR when the evidence and regression gates pass. Otherwise report no change. Never modify an installed plugin cache or advance the checkpoint after an incomplete scan.
